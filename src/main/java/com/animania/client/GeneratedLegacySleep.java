package com.animania.client;
import java.util.Map;
import net.minecraft.client.model.geom.ModelPart;
/** Generated from the LGPL-3.0 1.12 model sleeping branches. */
final class GeneratedLegacySleep {
static boolean apply(String key, Map<String, ModelPart> parts, float sleepTimer) {
switch (key) {
case "farm/client/model/cow/modelbull" -> {
			parts.get("Leg0").xRot = sleepTimer * -1.8F;
			parts.get("Leg1").xRot = sleepTimer * -1.8F;
			parts.get("Leg2").xRot = sleepTimer * 1.7F;
			parts.get("Leg3").xRot = sleepTimer * 1.75F;
			parts.get("Head").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelbullangus" -> {
			parts.get("Leg0").xRot = sleepTimer * -1.8F;
			parts.get("Leg1").xRot = sleepTimer * -1.8F;
			parts.get("Leg2").xRot = sleepTimer * 1.7F;
			parts.get("Leg3").xRot = sleepTimer * 1.75F;
			parts.get("Head").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelbullhereford" -> {
			parts.get("Leg0").xRot = sleepTimer * -1.8F;
			parts.get("Leg1").xRot = sleepTimer * -1.8F;
			parts.get("Leg2").xRot = sleepTimer * 1.7F;
			parts.get("Leg3").xRot = sleepTimer * 1.75F;
			parts.get("Head").yRot = sleepTimer * + 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelbulllonghorn" -> {
			parts.get("Leg0").xRot = sleepTimer * -1.8F;
			parts.get("Leg1").xRot = sleepTimer * -1.8F;
			parts.get("Leg2").xRot = sleepTimer * 1.7F;
			parts.get("Leg3").xRot = sleepTimer * 1.75F;
			parts.get("Head").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelcalf" -> {
			parts.get("Leg0").xRot = sleepTimer * -1.8F;
			parts.get("Leg1").xRot = sleepTimer * -1.8F;
			parts.get("Leg2").xRot = sleepTimer * 1.7F;
			parts.get("Leg3").xRot = sleepTimer * 1.75F;
			parts.get("Head").yRot = sleepTimer * + 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelcow" -> {
			parts.get("leg1").xRot = sleepTimer * -1.8F;
			parts.get("leg2").xRot = sleepTimer * -1.8F;
			parts.get("leg3").xRot = sleepTimer * 1.7F;
			parts.get("leg4").xRot = sleepTimer * 1.75F;
			parts.get("head").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelcowangus" -> {
			parts.get("leg1").xRot = sleepTimer * -1.8F;
			parts.get("leg2").xRot = sleepTimer * -1.8F;
			parts.get("leg3").xRot = sleepTimer * 1.7F;
			parts.get("leg4").xRot = sleepTimer * 1.75F;
			parts.get("head").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/cow/modelcowhereford" -> {
			parts.get("leg1").xRot = sleepTimer * -1.8F;
			parts.get("leg2").xRot = sleepTimer * -1.8F;
			parts.get("leg3").xRot = sleepTimer * 1.7F;
			parts.get("leg4").xRot = sleepTimer * 1.75F;
			parts.get("head").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbuckalpine" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbuckangora" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLegWool_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("FrontLegWool_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLegWool_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("BackLegWool_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbuckfainting" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbuckkiko" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbuckkinder" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbucknigeriandwarf" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelbuckpygmy" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoealpine" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoeangora" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLegWool_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("FrontLegWool_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLegWool_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("BackLegWool_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot =  + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoefainting" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoekiko" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot =  + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoekinder" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoenigeriandwarf" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot =  + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modeldoepygmy" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * 2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidalpine" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot =  (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidangora" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLegWool_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("FrontLegWool_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLegWool_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("BackLegWool_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidfainting" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot =  + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidkiko" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot =  - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidkinder" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = (float) Math.PI / 2F - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = (float) Math.PI / 2F + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidnigeriandwarf" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/goats/modelkidpygmy" -> {
			parts.get("FrontLeg_L").xRot = sleepTimer * -1.8F;
			parts.get("FrontLeg_R").xRot = sleepTimer * -1.8F;
			parts.get("BackLeg_L").xRot = sleepTimer * 1.7F;
			parts.get("BackLeg_R").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modeldorpersheep" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("HeadNode").yRot = sleepTimer * -2.8F;
			if (sleepTimer > -.28)
			{
				parts.get("Body").xRot = -(sleepTimer / 3);
			}
			else
			{
				parts.get("Body").xRot = +(sleepTimer / 3);
			}
return true; }
case "farm/client/model/sheep/modeldorsetewe" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * -4.5F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modeldorsetram" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * 4.0F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modelfriesiansheep" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftBackLegWool").xRot = sleepTimer * 1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("RightBackLegWool").xRot = sleepTimer * 1.8F;
			parts.get("HeadNode").yRot = sleepTimer * -4.5F;
			if (sleepTimer > -.28)
			{
				parts.get("Body").xRot = -(sleepTimer / 3);
			}
			else
			{
				parts.get("Body").xRot = +(sleepTimer / 3);
			}
return true; }
case "farm/client/model/sheep/modeljacobsheep" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * 4.0F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modelmerinoewe" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * -4.5F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modelmerinoram" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * 4.0F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modelsuffolkewe" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * -4.5F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
case "farm/client/model/sheep/modelsuffolkram" -> {
			parts.get("LeftFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightFrontLeg").xRot = sleepTimer * -1.8F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("LeftBackLeg").xRot = sleepTimer * 1.7F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("RightBackLeg").xRot = sleepTimer * 1.75F;
			parts.get("LeftFrontLegWool").xRot = sleepTimer * -1.8F;
			parts.get("HeadNode").yRot = sleepTimer * 4.0F;
			if (sleepTimer > -.28) {
				parts.get("Body").xRot = - (sleepTimer/3);
			} else {
				parts.get("Body").xRot = + (sleepTimer/3);
			}
return true; }
default -> { return false; }
}
}
}
